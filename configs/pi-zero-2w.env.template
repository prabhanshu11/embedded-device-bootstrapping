# Pi Zero 2W Configuration Template
# Copy to pi-zero-2w.env and fill in values
# DO NOT commit the .env file

# Device
PI_ZERO_HOSTNAME=pi-keyboard
PI_ZERO_PASSWORD=%%CHANGE_ME%%

# USB Gadget Network
PI_ZERO_IP=10.55.0.2
LAPTOP_IP=10.55.0.1

# WiFi (optional - if Pi needs WiFi in addition to USB)
WIFI_SSID=
WIFI_PASS=
